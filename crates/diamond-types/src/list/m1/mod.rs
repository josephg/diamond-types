/// This module contains my first (prototype) implementation for merging changes.
///
/// I still suspect this approach will end up faster and maybe a little smaller code-size wise, but
/// I haven't implemented it. This is not currently linked to the parent module!

mod merge;
pub mod yjsspan;
